"use strict";

{
    const SDK = self.SDK;

    const PLUGIN_CLASS = SDK.Plugins.admob_mediation_unityads;

    PLUGIN_CLASS.Type = class admob_mediation_unityadsType extends SDK.ITypeBase
    {
        constructor(sdkPlugin, iObjectType)
        {
            super(sdkPlugin, iObjectType);
        }
    };
}